{{- if and .Values.ingress.enabled .Values.skillsService.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "skills-stack.fullname" . }}-skills
  labels:
    {{- include "skills-stack.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.skills.host | quote }}
      http:
        paths:
          {{- range .Values.ingress.skills.paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "skills-stack.skillsService.fullname" $ }}
                port:
                  number: {{ $.Values.skillsService.service.port }}
          {{- end }}
{{- end }}
